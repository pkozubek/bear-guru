import React from "react";
import styles from "./logo.module.css";

const logo = props => {
  return (
    <h1 className={styles.Logo}>
      <span>Beer</span>Guru
    </h1>
  );
};

export default logo;
